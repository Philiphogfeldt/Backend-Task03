

/*document.getElementById("@(item.Name)Checkbox").addEventListener("change", function () {
    var label = document.querySelector(".beerFavoriteLabel");
    if (this.checked) {
        label.classList.add("checked");
    } else {
        label.classList.remove("checked");
    }
});*/

/*const checkbox = document.getElementById('@(item.Name)Checkbox');
const starUnselected = document.querySelector('.beerlyStar');
const starSelected = document.querySelector('.beerlyStar.checked');

checkbox.addEventListener('change', function () {
    if (checkbox.checked) {
        starUnselected.style.display = 'none';
        starSelected.style.display = 'grid';
    } else {
        starUnselected.style.display = 'grid';
        starSelected.style.display = 'none';
    }
});*/